# bittensor-style-ai-inference
An expert-level, Decentralized AI Inference network. Features a "Proof-of-Intelligence" reward mechanism where "Miners" provide GPU compute for model inference (e.g., LLMs) and "Validators" verify the quality of the output to distribute protocol incentives.
